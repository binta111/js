//number
const a = 100;
const b = 50;

let output;
output = a + b;
output = a - b;
output = a * b;
output = a / b;
output = a % b;

//math
output = Math.PI;
output = Math.round(2.2); 
output = Math.floor(2.8); //tala laijanako lagi
output = Math.ceil(2.1); //mathi laijanako lagi
output = Math.abs(-25); // neg lai pos maa laijana  
output = Math.sqrt(64); //root
output = Math.pow(2,3); //power
output = Math.random();
output = Math.random()*10;
output = Math.floor(Math.random()*10);
output = Math.min(2, 8, 5, -1, 9);
output = Math.max(2, 8, 5, -1, 9);
console.log(output);